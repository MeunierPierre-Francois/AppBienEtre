{% extends "base.html.twig" %}

{% block body %}
	<h1>{{ categorie.nom }}</h1>

	<p>{{ categorie.description }}</p>

	<h2>Prestataires proposant ce service :</h2>

	<ul>
		{% for p in proposer %}
			<li>{{ p.prestataire.nom }}</li>
		{% endfor %}
	</ul>
{% endblock %}
